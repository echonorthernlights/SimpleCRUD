@model SimpleCRUD.Web.Models.Person
@{
    ViewData["Title"] = "Delete Person Page";
}

<div class="container">
    <form method="post" asp-controller="People" asp-action="Delete" asp-route-id="@Model.Id">
        <h2>Are you sure you want to delete this record</h2>
        <h3>ID : @Model.Id</h3>

        <button class="btn btn-danger">Delete</button>
        @*  <a asp-controller="People" asp-action="Index">
        <input type="button" value="Go Back" class="btn btn-dark" />
        </a> *@
        <a asp-controller="People" asp-action="Index" class="btn btn-dark">
            Go Back
        </a>
    </form>

</div>
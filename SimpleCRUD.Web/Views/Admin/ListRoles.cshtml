@model IEnumerable<IdentityRole>;
@using Microsoft.AspNetCore.Identity;
@using SimpleCRUD.Web.Models.ViewModels;
@inject UserManager<ApplicationUser> UserManager;
@{
    var user = UserManager.GetUserAsync(User).Result;
}
@{
    ViewData["Title"] = "Roles Page";
}

<div class="container-fluid">
    <div class="text-center mb-3">
        <h1 class="display-4">List Of Roles</h1>
        @if (User.Identity.IsAuthenticated)
        {
            <a asp-controller="Admin" asp-action="CreateRole">
                <button class="btn btn-primary">Add Role</button>
            </a>
        }
    </div>
    <div class="row gap-2 justify-content-center">


        @foreach (var role in Model)
        {
            <div class="col-3 ">
                <div class="card">
                    <div class="card-body">
                        Role ID : <p class="card-title">@role.Id</p>
                        <h3 class="card-body">
                            @role.Name
                        </h3>
                        <div class="d-flex justify-content-center">
                            @if (User.Identity.IsAuthenticated)
                            {

                                <a class="nav-link text-dark" asp-area="" asp-controller="Admin" asp-action="EditRole" asp-route-id="@role.Id">
                                    <button type="button" class="btn btn-light btn-sm btn-dark"><i class="fa fa-list" aria-hidden="true"></i></button>

                                </a>

                                <a class="nav-link text-dark" asp-area="" asp-controller="Admin" asp-action="Delete" asp-route-id="@role.Id">
                                    <button type="button" class="btn btn-danger btn-sm"><i class="fa fa-trash" aria-hidden="true"></i></button>

                                </a>
                            }
                            else
                            {
                                <p><u>You need to login to perform operation(s)</u></p>
                            }




                            @*   <button type="button" class="btn btn-danger btn-sm"
                        data-bs-toggle="modal" data-bs-target="#deleteModal"
                        asp-controller="People" asp-action="Delete"
                        asp-route-id="@person.Id">Delete</button> *@
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>

    <!-- Modal -->
    @*  <div class="modal fade" id="deleteModal" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
    <div class="modal-dialog">
    <div class="modal-content">
    <div class="modal-header">
    <h1 class="modal-title fs-5" id="exampleModalLabel">Modal title</h1>
    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
    </div>
    <div class="modal-body">
    <p>
    Are you sure you want to delete this record
    Id :
    </p>
    </div>
    <div class="modal-footer">

    <a class="nav-link text-dark" asp-area="" asp-controller="People" asp-action="Delete">
    <button type="button" class="btn btn-danger">Delete</button>

    </a>
    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Close</button>

    </div>
    </div>
    </div>
    </div> *@

</div>

@model SimpleCRUD.Web.Models.ViewModels.CreateRoleViewModel
@{
    ViewData["Title"] = "Delete Role Page";
}

<div class="container">
    <form method="post" asp-controller="Admin" asp-action="Delete" asp-route-id="@Model.Id">
        <h2>Are you sure you want to delete this role ?</h2>
        <h3>ID : "@Model.Id </h3>

        <button class="btn btn-danger">Delete</button>
        @*  <a asp-controller="People" asp-action="Index">
        <input type="button" value="Go Back" class="btn btn-dark" />
        </a> *@

    </form>
    <a asp-controller="Admin" asp-action="ListRoles">
        <button class="btn btn-dark">Go Back</button>
    </a>
</div>